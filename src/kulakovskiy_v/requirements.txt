# BIOSEaRCH — Python dependencies (Python 3.10+)
# Install:  pip install -r requirements.txt
#
# Pin versions if you need byte-for-byte reproducibility; the ranges below are
# what the project was developed and tested against.

# --- core scientific stack ---
numpy>=1.24
pandas>=2.0
scipy>=1.10

# --- networks ---
networkx>=3.1

# --- machine learning ---
scikit-learn>=1.3
lightgbm>=4.0
node2vec>=0.4.6          # branch 2, step 1 (embedding); pulls in gensim

# --- plotting ---
matplotlib>=3.7
seaborn>=0.12
matplotlib-venn>=0.11    # branch 1, step 11 (Euler diagram)
plotly>=5.15             # branch 2, interactive 3D t-SNE (optional)

# --- notes ---
# * GNU parallel (system package, not pip) is needed for branch 1, step 4:
#       sudo apt install parallel
# * DIAMOnD (branch 1, branch C) runs under Python 2.7 and needs its own
#   environment — see 01_network_pipeline/README.md.
